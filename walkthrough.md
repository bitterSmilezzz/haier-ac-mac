# Haier AC Mac v1.9.48 发布与巡检演进报告

## 1. 概述与版本定位
- **版本号**：`v1.9.48`
- **发版主题**：自然语言午夜/正午钟点校准与逆序差刻解析、自动模式制冷潜热动力学全对称及状态栏设备矩阵实时感知
- **核心目标与架构演进**：
  1. **自然语言午夜与正午钟点校准、分钟丢失与“差分/差刻”倒算逆序时间解析缺陷根治**：
     - **午夜零点精准校准**：彻底修复午夜时段日常口语（“晚上12点”、“今晚12点”、“半夜12点”、“午夜12点”、“凌晨12点”、“今晚零点”、“晚上0点”）因原有 `isPM` 判断误累加 12 小时被严重误判为正午 12:00 的重大逻辑漂移缺陷，精准校准至当日/次日午夜 `00:00`；
     - **上午与正午界限对齐**：修复“中午11点”等口语被归入正午误加 12 小时变成深夜 23:00 的缺陷，规范上午/正午边界；
     - **“过分/过刻”分钟丢失缺陷修复**：修复日常习惯口语中带有连接词“过”（如“十点过五分”、“十点过十分”、“十点过一刻”、“十点过半”、“十点过三刻”）因“过”字阻断原有正则导致分钟信息丢失、被严重误判为整点（如 10:00）的缺陷；
     - **“差分/差刻”逆序倒算时间解析**：全面支持日常习惯口语“十点差五分”、“差五分十点”（精准折算为 09:55）以及“十点差一刻”、“差一刻十点”（精准折算为 09:45）等逆向差分倒算时间；
     - **钟点定时防倒计时抢占加固**：彻底阻断“定时在十点五分关机”等钟点定时被倒计时正则（`定时` + `分`）误抢占为“5分钟后关机”倒计时的缺陷。
  2. **自动模式制冷分支全气候环境湿度相变潜热动力学严格全对称**：
     - 在 `EnergyAnalyticsEngine.estimateInstantaneousPower` 的自动模式（`.auto`）制冷分支（`indoor >= target`）中，补齐蒸发器翅片水汽冷凝相变汽化潜热补偿：高湿重载环境（$\text{RH} \ge 65\%$）冷凝负荷动态补偿最高 +66W，干燥低湿环境动态调减负荷（最低 -20W），并在恒温维持态中注入 $0.4\times$ 阻尼补偿，将峰值功率上限统一定标为 1800W；
     - 达成智能自动模式制冷工况与独立制冷工况（`.cooling`）在热力学动力学仿真维度的 100% 物理严格全对称。
  3. **macOS 原生状态栏设备矩阵实时状态感知**：
     - 状态栏右键上下文菜单中的「空调设备控制矩阵」主入口标题全面对齐实时运行态：动态展示当前局域网在线台数与实时开机运行台数（例如 `空调设备控制矩阵 (3台在线，2台运行中)...`）；
     - 用户在状态栏右键展开菜单的一瞬间即可直观感知全屋空调在线与启停态势，交互感知力大幅跃升。

---

## 2. 关键架构变更与代码实现

### 2.1 钟点时间解析与逆序差刻引擎加固 (`VoiceCommandParser.swift` / `VoiceCommandParserTests.swift`)
- **午夜与半夜时段校准**：
  - 针对带有“晚上/今晚/半夜/午夜/凌晨”且小时为 12 或 0 的情况，精准设置 `hour = 0`，杜绝累加 12 错误；
- **差分与差刻倒算**：
  - 新增 `差\s*(\d+|一刻|三刻|半)\s*分?(?:钟)?\s*(\d+)点` 与 `(\d+)点\s*差\s*(\d+|一刻|三刻|半)\s*分?(?:钟)?` 正则，动态反算借位（如 10点差5分 -> 9点55分）；
- **过分与过刻解析**：
  - 匹配规则重构加入 `(?:过|零)?` 前缀，无缝捕获“十点过十分”、“十点过一刻”等习惯表述；
- **回归与边界保障**：
  - 在 `VoiceCommandParserTests` 中新增 `testScheduleTimeMidnightAndNoonAccuracy` 与 `testScheduleTimePastAndDifferentialMinutes`，覆盖 28 种复杂自然语言时间边界口语。

### 2.2 能耗动力学全气候热力学严格全对称 (`EnergyAnalyticsEngine.swift`)
- **自动模式制冷相变潜热补偿**：
  - 当自动模式根据温差判定进入制冷工况（`indoor >= target`）时，湿度 $\text{RH} \ge 65\%$ 动态触发蒸发器冷凝相变能耗补偿（最高 +66W），干燥低湿环境动态下调（最低 -20W），恒温维持态注入 $0.4\times$ 阻尼，与 `.cooling` 独立工况 100% 一致。

### 2.3 原生状态栏设备矩阵实时状态感知 (`StatusItemController.swift`)
- **在线台数与运行台数集成显示**：
  - 菜单初始化与动态刷新时统计 `onlineCount` 与 `runningCount`，直观呈现为 `空调设备控制矩阵 (\(onlineCount)台在线，\(runningCount)台运行中)...`。

---

## 3. 构建、测试与打包验证闭环
- **底层编译与语法校验**：
  - 运行 `SDKROOT=/Library/Developer/CommandLineTools/SDKs/MacOSX26.5.sdk swift build`，全模块编译 100% 通过（Build complete!）；
- **时间与逆序差刻独立单元验证**：
  - 执行独立验证测试脚本，验证午夜零点（00:00）、正午（12:00）、过分（10:05 / 10:10 / 10:15）、差分倒算（09:55 / 09:45）及防抢占逻辑，28 项用例全部 100% 通过；
- **应用打包与代码签名**：
  - 执行 `./build_app.sh 1.9.48` 打包，小组件（沙盒 + Application Support 只读例外）与主应用签名全部就绪；
  - 产出安装包：`dist/HaierAC-v1.9.48-macOS.zip`（2.7MB）。
