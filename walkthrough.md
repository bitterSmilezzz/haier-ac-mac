# Haier AC Mac v1.9.47 发布与巡检演进报告

## 1. 概述与版本定位
- **版本号**：`v1.9.47`
- **发版主题**：闭环自然语言“点五”时间防误降级为倒计时、风速口语与档位全映射、自动模式制热湿度动力学校准及状态栏协同视觉感知
- **核心目标与架构演进**：
  1. **自然语言钟点时间“点五”防误降级为倒计时缺陷根治**：
     - 彻底根除 `convertChineseNumbers` 中全局无上下文粗暴替换 `str.replacingOccurrences(of: "点五", with: ".5")` 导致的灾难性时间解析缺陷；以往日常高频的钟点预约口语“十点五分关机/开机”、“晚上8点5分关机/开机”、“十点五十分关机”等口语中，“点五”被破坏性替换为“.5”（变成“10.5分关机”或“10.5十分关机”），直接导致 `parseScheduleTime` 丢失钟点标志“点”而返回 `nil`，紧接着被 `parseCountdownMinutes` 提取出 `5分`，将钟点预约严重误判为“5分钟后关机/开机”倒计时；
     - 重构为带严格上下文界定的正则替换（`([一二两三四五六七八九\d]+)点五(?=度|°|个?小时|个钟头)`），仅在温度或小时等明确小数场景下才转为 `.5`，彻底杜绝钟点时间“分”被误伤；
     - 补齐“十点五分关机/开机”、“十点零五分”、“10点5分”、“十点五十分”、“全屋晚上8点5分关空调”等全套高频钟点防降级测试用例，守住分秒不差的预约安全防线。
  2. **自然语言风速口语盲区与档位全映射闭环**：
     - 彻底解决日常风速口语中动词间隔导致的解析盲区：全面支持“把风开大/风调大点/风开大点/把风开小/风调小点/风开小点/调大风速/调小风速/风大点/风小点”等高频习惯口语；
     - 补齐一至四档（1~4档、低中高档、极速风、慢速、柔风、静音等）档位口语解析，分别精准映射至微风、中风、强劲与自动档位；
     - 在 `AppModel.setWindSpeed` 中加入多级归一化匹配引擎，彻底修复以往传入档位别名时因未匹配选项描述而被错误回退为“0”（自动风）的严重体验缺陷，无论来自语音、Siri 快捷指令还是状态栏均能 100% 准确下发。
  3. **自动模式制热分支全气候环境湿度结霜化霜动力学校准**：
     - 在 `EnergyAnalyticsEngine.estimateInstantaneousPower` 的自动模式（`.auto`）制热分支（`indoor < target`）中，补齐环境湿度高湿结霜化霜补偿（$\text{RH} \ge 65\%$ 时最高 +75W）与干燥低湿热焓补偿（$\text{RH} \le 40\%$ 时最高 +30W），并在恒温维持态中引入湿热阻尼补偿；
     - 达成智能自动模式制热工况与独立制热工况（`.heating`）在热力学动力学仿真维度的 100% 全气候严格物理对称。
  4. **macOS 原生状态栏全屋风速协同视觉感知与运行台数精准对齐**：
     - 状态栏右键上下文菜单中的「🍃 全屋风速协同」级联子菜单全面对齐运行态感知：子菜单标题动态展示当前运行台数（例如 `(2台运行中)` 或 `(当前均未开机)`）；
     - 新增全屋协同一致性勾选反馈（`✓`）：当全屋所有运行中的空调当前风速一致时，自动在对应档位显示勾选标记，让全屋空调风速状态一目了然。

---

## 2. 关键架构变更与代码实现

### 2.1 钟点时间解析引擎与正则上下文加固 (`VoiceCommandParser.swift` / `VoiceCommandParserTests.swift`)
- **上下文受限小数替换**：
  - 弃用全局子串替换，采用前瞻正则 `#"([一二两三四五六七八九\d]+)点五(?=度|°|个?小时|个钟头)"#`，确保仅匹配“度/°/小时/个钟头”前置小数，彻底保护“点五分/点五十分”钟点时间；
- **钟点预约防降级回归保障**：
  - 在 `VoiceCommandParserTests` 中新增 `testScheduleTimePointFiveProtection` 测试，严密守护“十点五分”、“10点5分”、“晚上8点5分”、“十点五十分”等钟点定时准确解析至 `(hour, minute)`，不发生任何倒计时误判。

### 2.2 风速口语结构化解析与设备多级归一化 (`VoiceCommandParser.swift` / `AppModel.swift`)
- **动词间隔与档位扩充**：
  - `parseWindSpeed` 覆盖动词间隔口语（把风开大/小、风开大/小点、调大/小风速等）与全档位口语（1~4档、一至四档、低中高档、极速、慢速、柔风、静音）；
- **设备端智能映射**：
  - `AppModel.setWindSpeed` 内置 `normalizedSpeed` 计算逻辑，精准映射至设备数字模型的 options 选项或底层模式码（微风 1 / 中风 2 / 强劲 3 / 自动 0）。

### 2.3 能耗动力学全气候热力学严格对称 (`EnergyAnalyticsEngine.swift`)
- **自动模式制热高湿结霜化霜补偿**：
  - 当自动模式根据温差进入制热分支（`indoor < target`）时，湿度 $\text{RH} \ge 65\%$ 动态触发换热器结霜与高压化霜补偿（最高 +75W），恒温态注入 $0.4\times$ 维持阻尼，达成物理全对称。

### 2.4 原生状态栏协同视觉与运行台数感知 (`StatusItemController.swift`)
- **运行台数动态呈现**：
  - 全屋风速协同子菜单标题呈现 `(N台运行中)` 或 `(当前均未开机)`；
- **协同一致性勾选反馈**：
  - 动态计算运行中设备风速集合，全屋风速一致时在对应档位直观展示 `✓` 标记。

---

## 3. 构建、测试与打包验证闭环
- **底层编译与语法校验**：
  - 运行 `SDKROOT=/Library/Developer/CommandLineTools/SDKs/MacOSX26.5.sdk swift build`，全模块编译 100% 通过（Build complete!）；
- **时间与风速转换独立验证**：
  - 执行独立逻辑验证脚本，验证“十点五分关机”->“10点5分关机”、“二十六点五度”->“26.5度”、“1点5小时后关机”->“1.5小时”，各项测试均 100% 预期一致；
- **应用打包与代码签名**：
  - 执行 `./build_app.sh 1.9.47` 打包，小组件（沙盒 + Application Support 只读例外）与主应用签名全部就绪；
  - 产出安装包：`dist/HaierAC-v1.9.47-macOS.zip`（2.7MB）。
