# Haier AC Mac v1.9.46 发布与巡检演进报告

## 1. 概述与版本定位
- **版本号**：`v1.9.46`
- **发版主题**：闭环自然语言“X个小时”与两刻时间缺陷、制热全气候湿度动力学、状态栏风速协同矩阵与 Siri 风速快捷指令
- **核心目标与架构演进**：
  1. **自然语言“X个小时/半个小时”口语盲区与两刻钟点定时缺陷根治**：
     - 彻底根除日常口语中占比极高的小时倒计时失效盲区：重构 `VoiceCommandParser.parseCountdownMinutes` 中的 `hourPattern` 匹配规则，支持 `(?:个?小时|个钟头)`，解决以往“一个小时后关机”、“两个小时后关机”、“2个小时后关机”、“三个小时后关机”因包含“个”字全部解析失败（直接返回 `nil`）的重大缺陷；
     - 补齐“半个小时”-> 30分钟，覆盖日常高频“半个小时后开机/关机”；
     - 彻底根治钟点定时中因缺少两刻映射导致“十点两刻关机”、“十点二刻关机”、“十点2刻关机”被严重误判为 `10:02`（误差高达28分钟）的时间漂移缺陷，精准对齐映射为 10:30；
     - 补齐“二刻钟”-> 30分钟，以及口语中省略“钟”字的无量词刻数表达：“一刻后”（15分钟）、“两刻后/二刻后”（30分钟）、“三刻后”（45分钟）；
     - 扩充全屋快捷开关机谓词，“全关/全部关/全都关”与“全开/全部开/全都开”无缝映射至全屋电源控制。
  2. **制热工况全气候环境湿度热力学动力学校准与滤网机时量纲对齐**：
     - 在 `EnergyAnalyticsEngine.estimateInstantaneousPower` 的制热工况（`.heating`）中引入环境湿度热力学生态补偿：冬季高湿环境（$\text{RH} \ge 65\%$）模拟室外换热器结霜与高压化霜热负荷补偿（最高 +75W），干燥低湿环境（$\text{RH} \le 40\%$）动态补偿对流热焓维持功率（最高 +30W），与制冷潜热及自动模式达成 100% 物理对称；
     - 优化 `AppModel.estimatedFilterRemainingDays` 算法：日均负载统计优先采用真实设备机时 `totalDeviceMinutes` 结合设备台数计算，消除以往以墙钟时间估算导致的多设备平均机时系统性低估偏差。
  3. **macOS 原生状态栏全屋与各房间设备风速控制矩阵**：
     - 状态栏右键上下文菜单在全屋控制区新增「🍃 全屋风速协同」级联子菜单，直观提供“微风 (静音舒适)”、“中风 (适中循环)”、“强劲 (极速对流)”与“自动风速”统一调度；
     - 在“空调设备控制矩阵”各房间级联子菜单中，新增「🍃 调节风速 (当前: XX)」级联子菜单，支持随时快速调整单机风速档位；单设备模式顶层菜单同步支持快捷风速切换。
  4. **Siri / Shortcuts 快捷指令新增风速调节能力**：
     - 新增 `SetACWindSpeedIntent`，支持通过 Siri 语音或系统快捷指令自动化调整空调风速（如“用海尔空调设置风速为微风”、“全屋调节风速为强劲”），补全温控四大核心维度的系统级自动化能力。

---

## 2. 关键架构变更与代码实现

### 2.1 倒计时与钟点时间解析引擎加固 (`VoiceCommandParser.swift` / `VoiceCommandParserTests.swift`)
- **小时与量词容错匹配**：
  - 将 `hourPattern` 优化为 `#"(\d+(?:\.\d+)?)\s*(?:个?小时|个钟头)"#`，天然兼容有量词“个”与无量词的各类中文表达；
  - 增设“半个小时”映射至“30分钟”，彻底消除倒计时解析盲区；
- **钟点刻数与口语映射补全**：
  - 在 `convertChineseNumbers` 中增加“点两刻/时两刻/点二刻/时二刻/点2刻/时2刻”归一为“点30分”，根除误判为 10:02 的严重缺陷；
  - 增补“二刻钟”-> 30分钟，以及“一刻后/两刻后/二刻后/三刻后”等刻数倒计时口语映射；
- **全屋电源谓词扩充**：
  - `isAllPowerOff` 扩充“全关”、“全部关”、“全都关”；
  - `isAllPowerOn` 扩充“全开”、“全部开”、“全都开”；
- **单元测试保障**：
  - `VoiceCommandParserTests` 追加 24 项关键断言，覆盖刻数定时（s1~s8）、口语小时倒计时（cd1~cd13）与全关全开（po1~po4），100% 全部通过。

### 2.2 能耗动力学与滤网机时量纲校准 (`EnergyAnalyticsEngine.swift` / `AppModel.swift`)
- **制热工况多维湿度模型**：
  - 在 `.heating` 分支中引入 `heatHumComp` 算法：$\text{RH} \ge 65\%$ 时冷凝结霜恶化补偿最高 +75W，$\text{RH} \le 40\%$ 时干燥空气热焓维持补偿最高 +30W，并在稳态、低频过渡及重载温阶区间平滑引入；
- **滤网剩余寿命算法优化**：
  - 遍历近 14 天能耗记录时，优先读取 `$0.totalDeviceMinutes`，精准反映真实设备累计负荷。

### 2.3 原生状态栏风速控制矩阵 (`StatusItemController.swift`)
- **全屋风速协同**：
  - 状态栏右键主菜单全屋控制区新增「🍃 全屋风速协同」子菜单，支持一键切换微风、中风、强劲与自动档位；
- **设备矩阵风速级联**：
  - 每个房间级联子菜单与单设备顶层菜单均包含「🍃 调节风速 (当前: XX)」子菜单与当前档位勾选反馈。

### 2.4 Shortcuts / Siri 快捷指令拓展 (`AppIntents.swift`)
- **实现 `SetACWindSpeedIntent`**：
  - 接收 `windSpeed` 参数与可选 `deviceName` 参数（支持“全屋/全部”识别）；
  - 在 `ACAppShortcuts` 中注册短语，Siri 语音自然识别。

---

## 3. 构建、测试与打包验证闭环
- **独立断言验证**：
  - 运行独立断言套件，24 项断言全部 100% 通过（`🎉 ALL 24 VERIFICATION ASSERTIONS PASSED 100%!`）；
- **本地编译验证**：
  - 运行 `SDKROOT=/Library/Developer/CommandLineTools/SDKs/MacOSX26.5.sdk swift build`，0 错误编译通过（`Build complete!`）；
- **应用打包与签名**：
  - 运行 `./build_app.sh 1.9.46`，成功构建并签名 `dist/HaierAC.app`（含桌面小组件扩展），并生成发布压缩包 `dist/HaierAC-v1.9.46-macOS.zip`（2.7MB）。

---

## 4. 敏感数据安全审计
- 全仓扫描确认无真实手机号、明文密码、第三方 Secret Token 或私人凭据提交。
